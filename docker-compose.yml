version: "3.9"
services:
  backend:
    image: "vkmarket_backend:${TAG-latest}"
    env_file:
      - .env
    environment:
      PORT: 8888
    build:
      context: ./backend

  frontend:
    image: "vkmarket_frontend:${TAG-latest}"
    build:
      context: ./frontend
